[pytest]
markers =
    integration: requires live Ollama/Neo4j/Chroma services (skipped by default)
addopts = -m "not integration" --cov=. --cov-report=term-missing --cov-report=xml -v
testpaths = tests
